import Login from "./Login.tsx";

const LoginPage = () => {
    return (
        <div className="w-full h-full flex items-center justify-center p-5 lg:p-20">
            <Login/>
        </div>
    );
};

export default LoginPage;
